<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Valentine?</title>
  <style>
    body {
      margin: 0;
      height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      background: linear-gradient(135deg, #ff9a9e, #fad0c4);
      font-family: 'Poppins', sans-serif;
      overflow: hidden;
    }

    .card {
      background: white;
      padding: 35px;
      border-radius: 20px;
      text-align: center;
      box-shadow: 0 20px 40px rgba(0,0,0,0.2);
      max-width: 460px;
      z-index: 2;
    }

    h1, h2 {
      color: #ff4d6d;
    }

    p {
      color: #444;
      line-height: 1.6;
    }

    ul {
      text-align: left;
      padding-left: 20px;
    }

    .details {
      background: #fff0f3;
      border-radius: 15px;
      padding: 15px;
      margin-top: 20px;
      text-align: left;
    }

    .typing {
      margin-top: 18px;
      font-size: 18px;
      color: #ff4d6d;
      font-weight: bold;
      min-height: 24px;
    }

    .save-btn {
      margin-top: 20px;
      background: #ff4d6d;
      color: white;
      padding: 12px 26px;
      border: none;
      border-radius: 25px;
      font-size: 16px;
      cursor: pointer;
      transition: 0.3s ease;
    }

    .save-btn:hover {
      background: #e63950;
      transform: scale(1.05);
    }

    button {
      padding: 12px 28px;
      font-size: 18px;
      border: none;
      border-radius: 30px;
      cursor: pointer;
      transition: 0.3s ease;
      position: relative;
    }

    #yesBtn {
      background: #ff4d6d;
      color: white;
      margin-right: 15px;
    }

    #yesBtn:hover {
      background: #e63950;
      transform: scale(1.1);
    }

    #noBtn {
      background: #ccc;
      color: #333;
      position: absolute;
    }

    .hearts {
      position: fixed;
      top: -10px;
      font-size: 24px;
      animation: fall 4s linear infinite;
      z-index: 1;
    }

    @keyframes fall {
      0% { transform: translateY(0); opacity: 1; }
      100% { transform: translateY(110vh); opacity: 0; }
    }

    .music-hint {
      position: fixed;
      bottom: 15px;
      font-size: 14px;
      color: white;
      opacity: 0.8;
    }
  </style>
</head>
<body>

  <!-- 🎵 Background Music -->
  <audio id="bgMusic" loop>
    <source src="every-breath-you-take.mp3" type="audio/mpeg">
  </audio>

  <div class="card" onclick="startMusic()">
    <h1>Will you be my Valentine, Himanshi? 💖</h1>
    <button id="yesBtn" onclick="sayYes()">Yes 💘</button>
    <button id="noBtn">No 🙈</button>
  </div>

  <div class="music-hint">Tap anywhere for music 🎶</div>

  <script>
    const noBtn = document.getElementById("noBtn");
    const music = document.getElementById("bgMusic");

    noBtn.addEventListener("mouseover", moveButton);
    noBtn.addEventListener("click", moveButton);

    function moveButton() {
      const x = Math.random() * (window.innerWidth - 120);
      const y = Math.random() * (window.innerHeight - 60);
      noBtn.style.left = x + "px";
      noBtn.style.top = y + "px";
    }

    function sayYes() {
      document.body.innerHTML = `
        <div class="card">
          <h1>YAYYY 💕🥹</h1>
          <p>So it’s a <b>YES</b> 🫶  
          and that means I get to spoil you a little 💖</p>

          <h2>🍳 Breakfast Date Menu</h2>
          <ul>
            <li>🥗 Chicken Salad</li>
            <li>🍳 English Breakfast</li>
            <li>🍝 Chicken Pasta</li>
            <li>🍕 Pepperoni Pizza</li>
            <li>🍜 Spicy Ramen</li>
          </ul>

          <p><b>You choose whatever you like 😌</b></p>

          <h2>🍰 Desserts</h2>
          <ul>
            <li>🍫 Chocolate cake baked by <b>Shihoo</b></li>
            <li>❤️ Chocolate little hearts made by <b>me</b></li>
            <li>🍓 Served with fresh strawberries</li>
          </ul>

          <div class="details">
            <p><b>📍 Place:</b> Shihoo’s residence</p>
            <p><b>⏰ Time:</b> Anytime you like ❤️</p>
            <p><b>📅 Date:</b> 14th February 2026</p>
          </div>

          <div class="typing" id="typingText"></div>

          <button class="save-btn" onclick="saveDate()">💌 Save the Date</button>
        </div>
      `;
      createHearts();
      typeText("I choose you. ❤️");
    }

    function typeText(text) {
      const el = document.getElementById("typingText");
      let i = 0;
      const interval = setInterval(() => {
        el.textContent += text.charAt(i);
        i++;
        if (i === text.length) clearInterval(interval);
      }, 90);
    }

    function saveDate() {
      alert("💖 Date saved. I can’t wait to see you. 💖");
    }

    function createHearts() {
      setInterval(() => {
        const heart = document.createElement("div");
        heart.className = "hearts";
        heart.innerHTML = "💖";
        heart.style.left = Math.random() * 100 + "vw";
        document.body.appendChild(heart);
        setTimeout(() => heart.remove(), 4000);
      }, 300);
    }

    function startMusic() {
      music.play().catch(() => {});
    }
  </script>

</body>
</html>
